# =============================================================================
#  rover26 – Python dependencies
#
#  ROS 2 packages (rclpy, launch, launch_ros, interfaces, ament_*)
#  are installed via rosdep / apt and are NOT listed here.
#
#  Install:
#      pip install -r requirements.txt
# =============================================================================

# --- Runtime ------------------------------------------------------------------

# System metrics (CPU, RAM, temperature, disk, battery, WiFi)
psutil>=5.9.0

# Redis client – used by RoverLogger (stream), IHealthCheckable (health hashes),
# LoggerNode (XREAD), HealthMonitorNode (health registry)
redis>=4.0.0

# YAML config loading (Configurator)
PyYAML>=5.4.1

# --- Development / Test -------------------------------------------------------
pytest>=7.0.0
